name: CD

# TEMPORÁRIO: workflow_dispatch em vez de workflow_run, para testar de ponta
# a ponta antes de confiar no gatilho automático (ver RUNBOOK.md do rigst/ci,
# seção 7.5).

on:
  workflow_dispatch:

jobs:
  deploy:
    uses: rigst/ci/.github/workflows/deploy-django.yml@61e9d5135c491f2beb3b8f94b62dd476e2da9896  # v1
    secrets:
      CD_SSH_KEY: ${{ secrets.CD_SSH_KEY }}
    with:
      ssh-host: "vetorial.stolben.com"
